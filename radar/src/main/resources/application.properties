# Ativar o console web do H2 (Isso resolve o erro 404)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Configuração da conexão com o banco de dados em memória
spring.datasource.url=jdbc:h2:mem:radar_db
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Configurações do Hibernate para criar as tabelas automaticamente
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
